version: 2.1

orbs:
  yarn: artsy/yarn@4.0.0
  auto: artsy/auto@1.0.1

workflows:
  build:
    jobs:
      - yarn/workflow-queue:
          filters:
            branches:
              only: master
      - auto/publish-canary:
          context: npm-deploy
          filters:
            branches:
              ignore: master
          requires:
            - yarn/workflow-queue
      - auto/publish:
          context: npm-deploy
          filters:
            branches:
              only: master
          requires:
            - yarn/workflow-queue
